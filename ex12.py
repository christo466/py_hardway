# Prompt the user to enter their age, and capture the input in the 'age' variable
age = input("How old are you? ")

# Prompt the user to enter their height, and capture the input in the 'height' variable
height = input("How tall are you? ")

# Prompt the user to enter their weight, and capture the input in the 'weight' variable
weight = input("How much do you weigh? ")

# Print a formatted string that includes the user's age, height, and weight
print(f"So, you're {age} old, {height} tall and {weight} heavy.")
